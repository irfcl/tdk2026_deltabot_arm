version: '3.8'

services:
  # ninja-sima:
  #   build: .
  #   image: eurobot2026-ninja-sima-base:latest
  #   container_name: ninja-sima
  #   command: /bin/bash
  #   tty: true
  #   network_mode: host
  #   ipc: host
  #   privileged: true
  #   stdin_open: true
  #   env_file:
  #     - .env
  #   stop_grace_period: &STOP_GRACE_PERIOD 1s

  #   volumes:
  #     - ../NINJA-SIMA-ws:/home/user/NINJA-SIMA-ws
  #     - ./settings:/home/user/settings
  #     - /tmp/.X11-unix:/tmp/.X11-unix
  #     - $HOME/.Xauthority:/home/user/.Xauthority
  #     - /dev:/dev
  #     - /etc/timezone:/etc/timezone:ro
  #     - /etc/localtime:/etc/localtime:ro

  #   working_dir:
  #     /home/user/NINJA-SIMA-ws

  #   environment:
  #     - DISPLAY=${DISPLAY}
  #     - RCUTILS_COLORIZED_OUTPUT=1
  #     - XAUTHORITY=/home/user/.Xauthority
  #     - RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
  #     - CYCLONEDDS_URI=file:///home/user/settings/cyclonedds.xml

  uros-agent:
    image: microros/micro-ros-agent:humble
    container_name: uros-agent
    tty: true
    network_mode: host
    ipc: host
    privileged: true
    stdin_open: true
    
    env_file:
      - .env
      
    stop_grace_period: &STOP_GRACE_PERIOD 1s

    volumes:
      - /dev:/dev

    environment:
      # - RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
      - ROS_DOMAIN_ID=${ROS_DOMAIN_ID:-0}


    command: ["${AGENT_TYPE:-serial}", "--dev", "${AGENT_DEVICE:-/dev/ttyACM0}", "-b", "${AGENT_BAUD_RATE:-115200}"]